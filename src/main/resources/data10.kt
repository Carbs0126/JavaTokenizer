package cn.carbs.tokenizer.backup

import cn.carbs.ttt.R
import cn.carbs.ttt.R.layout
import cn.carbs.ttt.R.layout.my_textview

open class Test {

    fun tess() {
        val singleChar: Char = 'A'  // 单引号表示单个字符
        val str: String = "Hello"  // 双引号表示字符串

        // 有问题
        setContentView(R.layout
                .my_activity
        )
        val ss = "my_fragment_layout"
        val `var`: Int = my_textview
        setcontent(cn.carbs.tokenizer.R.layout.some_image_view)
    }
}