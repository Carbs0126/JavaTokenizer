
实践得来：

java 的 """ 内容必须在下一行写，内部可以用转义符，比如转义\"，单独 \" 其实和 " 作用相同，如果内部想要写 三个连着的 """，可以这样：
\""" 或者  \"\"\"，想要写 出 反斜杠，需要这样  \\


kotlin

val quote = "\"\"\""
    var x = """\
    \"\"\"tsdafd${quote}afaf
    """
    println(x)

    将会输出：
\
    \"\"\"tsdafd"""afaf

怎样输出单独一个$
    var x = """\
    \"\"\"tsdafd\${"$"}afaf
    """
    println(x)