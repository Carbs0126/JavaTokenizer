
1. 换行 token 不对
1. kotlin 的 import 丢了最后一个  解决，没有完全验证

// 下面虽然解析没有问题，但是没有将``中的内容解析为identifier？
    "Expected `$type`${if (!expectedResult) " not" else ""} to be a subtype of `$classId`$leniencyMessage (`$resultDirective`)."

// todo kotlin中 `` 在字符串外边成对出现，作为一整个 identifier


// todo range 有问题

range .. 两边不仅可以接 number，还可以接 任何 实现了 Comparable 接口的标识符

// number 添加一个表示是否已经有一个小数点，用来为后面 .. range 做准备


// java 中 还有 ...
// kotlin 中没有 ... 运算符，kotlin中用 vararg name: Type 表示

